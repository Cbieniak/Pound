<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorBackground">@color/background</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:actionBarStyle">@style/MyActionBar</item>


    </style>

    <style name="newTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorBackground">@color/polar_background</item>
        <item name="android:windowBackground">@color/polar_background</item>
        <item name="android:actionBarStyle">@style/PolarActionBar</item>
        <item name="android:l"></item>

    </style>

    

    <!-- ActionBar styles -->
    <style name="MyActionBar"
        parent="@android:style/Widget.Holo.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/highlight</item>
    </style>

    <style name="PolarActionBar"
        parent="@android:style/Widget.Holo.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/polar_highlight</item>
    </style>

</resources>
